receivers:
  otlp:
    protocols:
      http:
        endpoint: 0.0.0.0:4318

processors:
  batch:

exporters:
  debug:
    verbosity: detailed
  file/traces:
    path: /exported/traces.json
    # In collector 0.70.0+, non-rotation file writer is buffered and may not
    # flush within the test timeout. Explicit rotation keeps writes visible.
  file/metrics:
    path: /exported/metrics.json

service:
  telemetry:
    logs:
      level: "debug"
  pipelines:
    traces:
      receivers: [otlp]
      processors: [batch]
      exporters: [file/traces, debug]
    metrics:
      receivers: [otlp]
      processors: [batch]
      exporters: [file/metrics, debug]
